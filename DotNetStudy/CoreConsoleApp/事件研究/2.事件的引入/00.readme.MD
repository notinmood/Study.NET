# 说明

## 事件和委托的区别
- 委托是一个类
- 事件是根据委托生成的一个复杂对象实例
	- 这个事件实例不是委托的实例，而是跟委托类型有关联的一个对象。
	- 这个事件实例内包含一个订阅者列表，支持的订阅者为跟委托相同签名的方法
	- 这个事件实例内包含订阅方法、退订方法：可以方面讲订阅者中的订阅方法关联到这个列表上。


因为event生成的一个复合对象，因此不能使用赋值符号（=）,将同签名的方法赋值给事件对象；而是应该使用订阅/退订操作符（+=/-=），将同签名方法连接到event对象上。